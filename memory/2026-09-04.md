# Memory — 2026-09-04

## Cron Scheduler Diagnosis & Fix

### Issue Identified
- Heartbeat checks were flagging dream job failure and "all jobs show lastRun=never"
- Investigated via `openclaw cron list` — jobs WERE running (state.lastRunAt in jobs.json was unreliable)
- **Root cause 1 — Dream job:** Using `ollama/qwen3.6` (local model) which can't handle the complex multi-step tool-using protocol. Model "simulated" execution in text instead of actually using tools, got truncated (`stopReason: length`), never created the dream report file or posted to Discord.
- **Root cause 2 — Zillow scan:** Delivery channel was set to `"last"` instead of `"discord"`, causing delivery failures (`cron announce delivery failed`).

### Fixes Applied
1. **Dream job model:** Changed from `ollama/qwen3.6` → `ollama/glm-5.2:cloud` via `openclaw cron edit coo-dream-3am --model "ollama/glm-5.2:cloud"`
2. **Zillow delivery:** Changed channel from `"last"` → `"discord"` and added `--best-effort-deliver` via `openclaw cron edit d6cb34d6-d5dd-46de-a82b-c83baa82222a`
3. **HEARTBEAT.md:** Added note that `state.lastRunAt` field is unreliable; use `openclaw cron list` for accurate data

### Mason (CMO) — "From the Lab" LinkedIn Post
- Mason asked for shipped updates since last post (Aug 22)
- Confirmed his list: FOX Events (2 new, 199 dates normalized, 12 rows fixed), config governance rule (36 agents)
- Added: Zillow scan refinement (Aug 21), R2D2 dedup hardening
- Flagged: LinkedIn token still expired (401 since July 14) — asked Mason if Spock refreshed it

### Open Items
- Dream job fix needs validation — next run at 3 AM Sep 5 will confirm if cloud model creates the report file
- Zillow job error count (2) should reset on next successful run tomorrow at 9 AM
- 2 uncommitted workspace changes (HEARTBEAT.md edit + this memory file)