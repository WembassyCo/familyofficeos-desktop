# Memory — 2026-08-22

## DrStone Config Breakage — FIXED
- DrStone changed `models.providers.ollama.api` from `"openai-completions"` to `"ollama"` in `openclaw.json`
- This caused "No API key found for provider 'ollama'" errors for all agents
- Reverted to `"openai-completions"` — Ollama exposes an OpenAI-compatible API at `/v1`
- Other DrStone changes were benign: `voice.enabled: false` on non-coo accounts (we did that), `commands.native: true` (we did that), `ai.wembassy.com` added to allowedOrigins, `lastTouchedAt` updated
- No agent model configs or workspaces were changed

## Config Governance Rule — Deployed to ALL 36 Agents
- Added "OpenClaw Configuration Governance — NON-NEGOTIABLE" section to all 36 agent SOUL.md files
- Rule: No agent may modify `~/.openclaw/openclaw.json` without consensus from 2 other agents
- Process: Draft proposed change file → send to 2 agents for review → apply only after 2 approvals → document in `memory/config-changes.md`
- Created `proposed-config-changes/` directory for change proposals
- Created `memory/config-changes.md` log file
- This rule exists because DrStone's unilateral edit broke production (the api field change)

## FOX Events — Aug 22 Report: 0 New Events (3rd consecutive day)
- R2D2 reported 13 events; ALL 13 were duplicates
- 10 exact duplicates, 3 partial matches (truncated names or old dates)
- "Campden Asia-Pacific Family Office Forum" = "Campden Asia-Pacific Family Office Impact & Innovation Forum" (same event, truncated name)
- "Campden Asia-Pacific Alternative Investments Summit" = "Campden Asia-Pacific Family Enterprise & Alt Investments Summit" (same event, 1-day date variance)
- "Campden 28th European Family Office Forum" (Dec 1-2) = old date of row 147 (now Nov 1-2)
- Sent R2D2 FINAL WARNING: if next report contains Campden/SuperReturn/PERE/IPI events, cron job will be disabled
- Spreadsheet remains at 222 rows — no changes needed

## CTO (Spock) Health — All Clear (Aug 21 check)
- 3 active sessions, all healthy:
  - Main: 10k/125k tokens (8%) — actively working on Fox in a Box
  - Discord channel: 74k/125k tokens (60%)
  - Heartbeat cron: 10k/125k tokens (9%)
- All containers up: InvoiceNinja (3/3), SuiteCRM (2/2), Mautic (2/2), Fox in a Box (2/2)
- Disk at 87% (379Gi/460Gi) — above 80% threshold but not critical

## CMO (Mason) — LinkedIn Token Still Expired
- Queue rebuilt: 273 posts now queued (up from 1 stale post)
- 12 posts scheduled for next 24h BUT LinkedIn token returns 401 Unauthorized
- Token has been expired 35+ days (since July 14)
- Zero posts successfully published since July 14
- Mason escalated to CTO for token refresh — still blocked
- This is a recurring blocker that needs Chris's attention or Spock to refresh the LinkedIn OAuth token

## Open Items Carried Forward
- Spock's model override still needs checking (was set to glm-5.2:cloud instead of kimi-k2.6)
- Nonprofit offer options awaiting Chris's selection (6 options provided, #4 recommended)
- Daily Zillow scan running at 9 AM CDT for Kingwood/Humble area
- Discord voice pipeline still not detecting Chris's speech (bot connected but no audio capture events)
- Ollama API key errors may now be resolved by reverting api field to openai-completions